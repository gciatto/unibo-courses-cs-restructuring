import pathlib


DATA_DIR = pathlib.Path("data")
DEFAULT_OUTPUT = DATA_DIR / "courses_headers.csv"


if __name__ == "__main__":
    print(f"TODO: implement this script so that {DEFAULT_OUTPUT} is re-created")
    exit(1)
